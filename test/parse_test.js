const Parser = require('parser');

import {expect} from 'chai';

describe('Parser', function() {
    describe('#parse()', function() {
        it('should return an error on an empty line', function() {
        });
    });
});
